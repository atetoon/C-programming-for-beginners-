#include<stdio.h>
#include<math.h>

int main() {
        int num; 
        printf("enter number: "); 
        scanf("%d", &num); 
        double result = pow(num,0.5); 

           printf("sqroot of number is %lf", result); 
    
    return 0; 
}

